'use client'
// import { useWindowSize } from '@/hooks/use-get-window-size'

export const HappyComponent = () => {
  // 'use client'
  // const windowSize = useWindowSize()
  const windowSize = 'happy'
  return <p>Happy component {windowSize}</p>
}
